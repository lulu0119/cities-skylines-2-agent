# M1 Windows in-game smoke (archived)

**Date:** 2026-08-06  
**Status:** Closed as a checklist — results frozen below. Live onboarding is under `docs/guide/`.

## Results

| 项 | 状态 | 备注 |
|---|---|---|
| 脚手架 `Mod/` + `UI/` | ✅ | `dotnet new csiimod` + `create-csii-ui-mod`；面板曾用 `GameBottomRight` + `Portal` |
| 构建 / 部署 | ✅ | `dotnet build` → Mods\\CitiesSkylines2Agent\\ |
| mock LLM `:8787` | ✅ | `archive/mock` |
| **3.1** Gameface fetch / stream | ⚠️ | **stream ❌**：`ReadableStream undefined in Gameface`；非流式 fetch 未最终钉死 |
| **3.2** C# HTTPS | ✅ | `ok status=401 Unauthorized`（假 key；TLS OK） |
| **3.3** UIUpdate 队列 | ✅ | 暂停下 `queue: ok … speed=0` |

## Conclusions (at the time)

- C# HTTPS + paused UIUpdate queue are viable.
- Gameface lacks `ReadableStream` → stock apeira/xsai stream path blocked; agent loop tentatively **C# `IChatClient` + hand ReAct**.
- Do not append UI to bare `Game` (collides with developerMode F/S/H/Q strip).

## Original procedure (historical)

### 3.1 Gameface fetch / ReadableStream

- UI button: `fetch('http://127.0.0.1:8787/v1/chat/completions', …)` and stream via `response.body.getReader()`.

### 3.2 In-mod HTTPS

- C# `HttpClient` → `https://api.openai.com/v1/models` with fake key (401 = TLS OK).

### 3.3 UIUpdate queue while paused

- CS2MCP-style queue on `SystemUpdatePhase.UIUpdate`.
