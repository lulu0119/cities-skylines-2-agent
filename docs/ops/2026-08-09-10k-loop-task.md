# 任务：把游戏内 agent 跑到 10k 人口（迭代开发闭环）

## 你的总目标

通过“启动游戏 → 观测 → 分析日志 → 最小修改 → 部署 → 再启动”的循环，
让游戏内 agent 自主把存档城市推到 **人口 ≥ 10,000** 且核心服务稳定
（problems[] 无 critical/high、城市不破产、收入为正）。

**重要区分**：10k 是你的验收目标，不是产品目标。
游戏内 agent 的目标永远是通用的：
`Build the city, grow population, solve problems. Keep working until the city thrives.`
**不要把 10k 写进代码、System prompt、skill 或默认设置。**

## 工作区与代码现状（不要破坏）

仓库：`C:\Users\super\Documents\GitHub\cities-skylines-2-agent`

main 已包含：profile 拆分/设置页/auto-start（会话 A），以及污水闭环改动
（problems[]/topIssues、wait_simulation、超时、city-building skill）——
均已提交（当前 HEAD = `4cd5dbb`）并已 `dotnet build` 部署到
`%LocalLow%\Colossal Order\Cities Skylines II\Mods\CitiesSkylines2Agent\`。
不要 git reset、不要重写历史文档（docs/ops 下的旧 handoff 是历史记录）。

当前已定死的行为（不得回退）：

- agent 对模拟只有 `wait_simulation(seconds)`（1–60s，内部最高速，
  结束恢复玩家原速度/暂停状态）；没有 set_simulation、没有 agent_advance_time、
  没有自动推进。
- 自动续跑只发文本：`Build the city, grow population, solve problems. Keep working until the city thrives.`
- 模型生成超时 120s：不重试，结束整轮（含自动续跑），agent 保持 idle。
- System prompt 是通用原则：先修限制发展的问题、按地图选型、不规定套路。
- `find_placement` 成功后同轮 `place_building` 是 prompt 规则（当前非代码强制）。
  **如果日志再次出现“找到位置后回合直接结束”，就把这条改成代码强制**：
  执行器记录最后一次成功调用的 find_placement，回合要结束时若没有对应
  place_building，自动追加一条继续消息，而不是让回合结束。

## 修改范围

允许改：`Mod/Agent/*`、`Mod/Agent/Profiles/*`、`Mod/Agent/Skills/*/SKILL.md`、
`Mod/Agent/ToolCatalog.json`、`Mod/CS2MCP/*`、`Mod/Setting.cs`；
确属 UI 问题时才碰 `Mod/UI/src/mods/chat-panel.tsx`；诊断/交接笔记新建到
`docs/ops/YYYY-MM-DD-<slug>.md` 并挂到 docs/README.md。

禁止：改 UI 挂载方式（GameBottomRight + Portal）、往
`Mods\CitiesSkylines2Agent` 目录写任何运行时文件（黑屏根因）、动 archive/、
改历史文档、把 API key 放进代码/日志/文档。

遵循 AGENTS.md：最小正确 diff、无 drive-by 重构、一次只改一个假设、
每次逻辑改动后 `dotnet build` 必须 0 错误。

## 构建与部署

- 关闭游戏后再构建（DLL 被占用会失败/损坏）。
- `cd Mod && dotnet build` —— 自动部署到
  `%LocalLow%\Colossal Order\Cities Skylines II\Mods\CitiesSkylines2Agent\`、
  合并依赖、构建 UI。UI-only 改动才需要 `cd Mod/UI && npm run build`
  （需要 CSII_USERDATAPATH）。

## 启动游戏（全流程参考 docs/ops/2026-08-08-windows-mcp-game-debug-loop.md）

1. Windows-MCP：`http://127.0.0.1:8000/mcp`，先 initialize 拿 `mcp-session-id`，
   tools/list；第一个动作永远是 Snapshot，不要盲点。
2. 启动：`Start-Process -FilePath 'C:\Users\super\scoop\apps\steam\current\steam.exe' -ArgumentList '-applaunch 949230 -uiDeveloperMode'`。
3. Snapshot 找到 Paradox Launcher 的 Play 按钮 → Click → Wait →
   确认 `Cities2.exe` 存在且前台。
4. 主菜单用 Gameface CDP `:9444`（json/list）找本地化文本“继续游戏”/
   “Continue Game”，dispatch `mousedown`+`mouseup`+`click`，等城市加载完成。
5. 面板和输入框用 CDP 探到坐标后，用 Windows-MCP `Type`（clear=true）+ Enter/Send；
   不要用 `input.value=` 赋值（React 会吞掉）。
6. 城市加载后 agent 应自动开局（AutoStart 只发一次）；不要手动多发一条。

## 观测

- agent 时间线：`<runtime root>\CitiesSkylines2Agent\logs\agent-timeline-*.jsonl`
  （runtime root = CSII_USERDATAPATH，未设置则为
  `%USERPROFILE%\AppData\LocalLow\Colossal Order\Cities Skylines II`）。
- 游戏日志：同根 `Logs\UI.log`、`Logs\CitiesSkylines2Agent.Mod.log`；
  仓库根 `debug-548a1a.log`（会话 NDJSON）。
- 用 PowerShell 定向取样（Select-String 按时间窗/关键字），不要把整个日志贴进对话。
- 每轮记录：人口/钱/XP、agent 回合数、工具成功失败、errors、timeout、
  UI.log 是否出现重复 `Reloading media 0`（黑屏信号）、面板是否正常。
- 已知失败模式，看到就要修：
  1. `find_placement` 成功但回合结束没 `place_building`（→ 做代码强制）；
  2. 反复重试同一坐标/同一工具（Road OverlapExisting 循环）；
  3. 污水/水/电容量为 0 但 agent 去建路/分区；
  4. 模型思考期间无任何事件且 >120s（超时事件应出现）；
  5. 回合结束无总结（MaxToolRounds 应有可见提示）。

## 结束游戏与修改

- 每次改代码前先干净退出游戏（窗口正常关闭/Process 确认停止），
  再 `dotnet build`，再按同样路径启动，**同一复现方式验证同一个假设**。
- 一次只改一个问题；改完一轮必须验证上一轮行为没有被破坏。

## 循环与验收

循环：启动 → 观察一轮/多轮 agent 行为 → 从日志定位最大问题 →
关游戏 → 最小修改 → 构建部署 → 再启动。

验收：`city_overview.population >= 10000`，连续多次 `problems[]` 无
sewage/water/electricity/garbage 的 critical/high，money 不持续为负。

每轮迭代用时间戳记录（launch/observe/diagnose/change/build/relaunch），
最后交一份证据报告：时间线关键片段、人口增长曲线、每次改动的文件与理由、
以及“为什么这次改动不会引发黑屏”（无 Mods 内运行时写入、无每帧 UI 事件、
不阻塞主线程）。
